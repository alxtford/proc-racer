{
  "name": "proc-racer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npm run serve",
    "serve": "python -m http.server 4173",
    "validate": "npm run validate:content && npm run validate:garage",
    "validate:content": "node scripts/validate-content.mjs",
    "validate:garage": "node scripts/check-garage-loop.mjs"
  },
  "dependencies": {
    "playwright": "^1.58.2"
  }
}
